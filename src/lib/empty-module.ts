// This module is intentionally left empty.
// It's used as a webpack alias for modules that should not be bundled on the client-side,
// such as the 'canvas' module which pdfjs-dist might try to import.
export {};
